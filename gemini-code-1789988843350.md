# Personal Algo-Trading Bot
**Project Architecture & Implementation Plan**

## 1. Project Overview
- **Objective:** สร้างระบบเทรดหุ้นสหรัฐฯ อัตโนมัติ (Automated Trading Bot) สำหรับพอร์ตขนาด $700 
- **Target Assets:** หุ้นสหรัฐฯ ที่มีความผันผวนสูง (เช่น NVDA, TSM)
- **Core Principle:** ทำงานอัตโนมัติ 24/7 บนเซิร์ฟเวอร์ โดยใช้ Technical Analysis เปิดสัญญาณ, ตรวจสอบข่าวด้วย AI, ควบคุมความเสี่ยงด้วย Risk Engine ก่อนส่งคำสั่งซื้อขายแบบแบ่งไม้ (Scaling) 

## 2. Technology Stack
- **Broker & API:** Alpaca Trading API (รองรับ Paper Trading, Zero Commission, Fractional Shares)
- **Backend / Logic:** Python 3.x
- **Database / State Management:** PostgreSQL (เก็บบันทึก Order, Portfolio State, Logs)
- **Infrastructure:** Ubuntu VPS
- **Process Manager:** PM2 (สำหรับรัน Background Process และ Auto-restart)
- **AI / Sentiment Analysis:** OpenRouter API หรือ Local LLM (Ollama)
- **Monitoring (Optional/Future):** Next.js หรือ Angular คู่กับ Nginx สำหรับ Dashboard และ Kill Switch

## 3. System Architecture & Core Loop
ระบบทำงานตามลำดับ Pipeline ดังนี้:
1. `Market Data`: ดึงข้อมูลราคาและ Indicator 
2. `Technical Analysis`: ตรวจสอบเงื่อนไขการเข้า Trade
3. `AI Sentiment (Gatekeeper)`: ดึงข่าวล่าสุดวิเคราะห์หาความเสี่ยงเชิงลบ
4. `Risk Engine & Position Sizing`: คำนวณขนาดไม้ (เช่น ไม้ละ $20) และตรวจสอบ Max Drawdown
5. `Order Execution`: ส่งคำสั่งซื้อไปยัง Alpaca พร้อมแนบ Bracket Order (Take Profit & Stop Loss) ทันที
6. `State Management`: บันทึกสถานะการทำรายการลง PostgreSQL
7. `Monitoring`: รอสัญญาณรอบถัดไป

## 4. Implementation Phases (MVP Approach)
แผนการพัฒนาแบ่งเป็นเฟสเพื่อให้ AI ทยอยสร้างและทดสอบทีละส่วน:

### Phase 1: Foundation & Market Data (API Connection)
- [ ] Set up Python environment & dependencies (`alpaca-trade-api`, `psycopg2`, `pandas`).
- [ ] Set up PostgreSQL database & schema สำหรับเก็บ `trades_log` และ `system_state`.
- [ ] เขียนโมดูลเชื่อมต่อ Alpaca API (Paper Trading) และทดสอบดึงข้อมูลราคา (REST/WebSockets) แบบ Real-time.

### Phase 2: Core Trading Logic & Order Execution (MVP)
- [ ] เขียนฟังก์ชันคำนวณ Technical Indicators พื้นฐาน (เช่น EMA, RSI, Support/Resistance).
- [ ] เขียนฟังก์ชันยิงคำสั่งซื้อ (Buy Order) แบบจำกัดงบประมาณ.
- [ ] **Critical:** ผูก Bracket Order (Take Profit / Stop Loss) เข้ากับทุกคำสั่งซื้ออัตโนมัติ.

### Phase 3: State Management & Risk Control
- [ ] เขียนลอจิกให้ Bot บันทึกและอ่าน State จาก PostgreSQL เพื่อให้สามารถ Recover ได้หาก PM2 สั่ง Restart สคริปต์.
- [ ] เขียน Risk Engine ดักจับ (Fail-safe) เช่น ฆ่า Process หาก Daily Loss ถึงขีดจำกัด.

### Phase 4: AI Sentiment Integration 
- [ ] เขียนสคริปต์ดึงข่าว (News API) ของหุ้นเป้าหมาย.
- [ ] เชื่อมต่อ OpenRouter API / Ollama ประเมิน Sentiment Score เพื่ออนุมัติหรือระงับสัญญาณจาก Phase 2.

### Phase 5: Paper Trading & Deployment
- [ ] Deploy โค้ดทั้งหมดขึ้น Ubuntu VPS.
- [ ] รันสคริปต์ผ่าน PM2 (`pm2 start bot.py`).
- [ ] ทดสอบระบบในสภาวะตลาดจริงด้วยบัญชี Paper Trading เป็นเวลา 1-2 สัปดาห์.

## 5. Strict Constraints & Security (Fail-Safes)
- **API Scoping:** กำหนดสิทธิ์ API Key เป็น `Trade Only` (ห้ามมีสิทธิ์ Withdraw เด็ดขาด).
- **Hard-coded Limits:** งบประมาณต่อไม้และขาดทุนสูงสุดรายวัน ต้องถูก Hard-code ไว้ใน Risk Engine.
- **Stateless Execution:** ตัวสคริปต์ Python ต้องไม่เก็บสถานะไว้ใน Memory นานเกินไป ให้พึ่งพาค่าจาก Database เป็นหลัก เพื่อป้องกันความผิดพลาดเมื่อ Server รีบูต.
- **Kill Switch:** ต้องมีกลไก (เช่น API Endpoint หรือแก้ Flag ใน Database) เพื่อระงับการทำงานของบอทได้ทันทีเมื่อเกิดเหตุฉุกเฉิน.